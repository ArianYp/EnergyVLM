#!/usr/bin/env bash
#BSUB -J sd-W-acc-smoke
#BSUB -G s10396
#BSUB -q training-parallel
#BSUB -n 4
#BSUB -W 0:40
#BSUB -R "span[hosts=1]"
#BSUB -R "select[mem>128000]"
#BSUB -R "rusage[mem=128000]"
#BSUB -M 128000
#BSUB -gpu "num=1:mode=exclusive_process:gmem=78G"
#BSUB -cwd /lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM
#BSUB -o /lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM/logs/lsf-sd-W-acc-smoke-%J.out
#BSUB -e /lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM/logs/lsf-sd-W-acc-smoke-%J.err
# 30-step smoke test of --ema_decay: EMA files written, EMA weights close to but not equal to the raw weights.
set -euo pipefail
export PATH="/software/cellgen/team361/ha11/envs/nichejepa/bin:$PATH"
export HF_HOME=/lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM/cache/huggingface
export TORCH_HOME=/lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM/cache/torch
export XDG_CACHE_HOME=/lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM/cache/xdg
export TOKENIZERS_PARALLELISM=false
export WANDB_MODE=disabled
export PYTHONPATH="/lustre/scratch126/cellgen/lotfollahi/ha11/EnergyVLM:${PYTHONPATH:-}"
source ~/.secrets/hf_token
OUT=checkpoints/phaseW_smoke/acc_smoke_${LSB_JOBID}
python3 phaseW/train_pilot_frozen_acc.py --variant CD_dinop_hard --coupling fresh --distill_loss x0_huber \
  --seed 0 --sel_seed 0 --selection_dir phaseN/coco_selection_dinopatch --alpha 1.0 --temp 1.0 \
  --N 4 --K 8 --cfg 7.0 --student_cfg 0.0 --cfg_mode composed --num_steps 12 --num_warmup_steps 2 --lr 1e-5 \
  --weight_decay 0.0 --grad_clip 1.0 --height 512 --sched_shift 3.0 --num_train_ts 1000.0 --delta_min 1 --delta_max 3 \
  --scoring_window 0.4,0.9 --support_m 1 --assign_seed 982451653 --huber_c 0.0 --gradient_checkpointing \
  --save_every 4 --log_every 2 --sample_every 0 --accum 4 --no_wandb --output_dir "$OUT"
python3 - "$OUT" <<'PY'
import sys, torch, glob
from pathlib import Path
out = Path(sys.argv[1]); fs = sorted(p.name for p in out.glob("*.pt")); print(fs)
ck = torch.load(out / "checkpoint_final.pt", map_location="cpu", mmap=True, weights_only=False)
assert ck["step"] == 12, ck["step"]
assert "checkpoint_step4.pt" in fs and "checkpoint_step8.pt" in fs, fs
print("ACCUM_SMOKE_OK: 12 updates with accum 4, checkpoints at 4/8/final")
PY
echo "[$(date --iso-8601=seconds)] smoke done: $OUT"
